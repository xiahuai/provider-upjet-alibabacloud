---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clusters.polardb.alibabacloud.crossplane.io
spec:
  group: polardb.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alicloud
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the Clusters API. Provides a PolarDB
          cluster resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of Cluster
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowShutDown:
                    description: 'Specifies whether to enable the no-activity suspension
                      feature. Default value: false. Valid values are true, false.
                      This parameter is valid only for serverless clusters.'
                    type: string
                  autoRenewPeriod:
                    description: Auto-renewal period of an cluster, in the unit of
                      the month. It is valid when pay_type is PrePaid. Valid value:1,
                      2, 3, 6, 12, 24, 36, Default to 1.
                    type: number
                  backupRetentionPolicyOnClusterDeletion:
                    description: The retention policy for the backup sets when you
                      delete the cluster.  Valid values are ALL, LATEST, NONE. Value
                      options can refer to the latest docs DeleteDBCluster
                    type: string
                  cloneDataPoint:
                    description: |-
                      The time point of data to be cloned. Valid values are LATEST,BackupID,Timestamp.Value options can refer to the latest docs CreateDBCluster CloneDataPoint.
                      -> NOTE: If CreationOption is set to CloneFromRDS, the value of this parameter must be LATEST.
                    type: string
                  collectorStatus:
                    description: Specifies whether to enable or disable SQL data collector.
                      Valid values are Enable, Disabled.
                    type: string
                  compressStorage:
                    description: |-
                      Enable storage compression function. The value of this parameter is ON. Only MySQL supports.
                      -> NOTE: When the value of db_type is not MySQL, the value of creation_option is neither empty nor Normal, and the value of storage_type is not PSL4, this field will be ignored.
                    type: string
                  creationCategory:
                    description: |-
                      The edition of the PolarDB service. Valid values are Normal,Basic,ArchiveNormal,NormalMultimaster,SENormal.Value options can refer to the latest docs CreateDBCluster CreationCategory.
                      -> NOTE: You can set this parameter to Basic only when DBType is set to MySQL and DBVersion is set to 5.6, 5.7, or 8.0. You can set this parameter to Archive only when DBType is set to MySQL and DBVersion is set to 8.0. From version 1.188.0, creation_category can be set to NormalMultimaster. From version 1.203.0, creation_category can be set to SENormal.
                    type: string
                  creationOption:
                    description: 'The method that is used to create a cluster. Valid
                      values are Normal,CloneFromPolarDB,CloneFromRDS,MigrationFromRDS,CreateGdnStandby,RecoverFromRecyclebin.
                      NOTE: From version 1.233.0, creation_option can be set to RecoverFromRecyclebin.
                      Value options can refer to the latest docs CreateDBCluster CreationOption.'
                    type: string
                  dbClusterIpArray:
                    description: db_cluster_ip_array defines how users can send requests
                      to your API. See db_cluster_ip_array below.
                    items:
                      properties:
                        dbClusterIpArrayName:
                          description: |-
                            The name of the IP whitelist group. The group name must be 2 to 120 characters in length and consists of lowercase letters and digits. It must start with a letter, and end with a letter or a digit.
                            NOTE: If the specified whitelist group name does not exist, the whitelist group is created. If the specified whitelist group name exists, the whitelist group is modified. If you do not specify this parameter, the default group is modified. You can create a maximum of 50 IP whitelist groups for a cluster.
                          type: string
                        modifyMode:
                          description: |-
                            The method for modifying the IP whitelist. Valid values are Cover, Append, Delete.
                            NOTE: There does not recommend setting modify_mode to Append or Delete and it will bring a potential diff error.
                          type: string
                        securityIps:
                          description: List of IP addresses allowed to access all
                            databases of a cluster. The list contains up to 1,000
                            IP addresses, separated by commas. Supported formats include
                            0.0.0.0/0, 10.23.12.24 (IP), and 10.23.12.24/24 (Classless
                            Inter-Domain Routing (CIDR) mode. /24 represents the length
                            of the prefix in an IP address. The range of the prefix
                            length is [1,32]).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  dbMinorVersion:
                    description: Database minor version. Value options can refer to
                      the latest docs CreateDBCluster DBMinorVersion. This parameter
                      takes effect only when db_type is MySQL and db_version is 8.0.
                    type: string
                  dbNodeClass:
                    description: |-
                      The db_node_class of cluster node.
                      -> NOTE: Node specifications are divided into cluster version, single node version and History Library version. They can't change each other, but the general specification and exclusive specification of cluster version can be changed.
                      From version 1.204.0, If you need to create a Serverless cluster with MySQL , db_node_class can be set to polar.mysql.sl.small.
                      From version 1.229.1, If you need to create a Serverless cluster with PostgreSQL 14 using the SENormal edition, db_node_class can be set to polar.pg.sl.small.c(x86 Architecture). Region can refer to the latest docs(https://help.aliyun.com/zh/polardb/polardb-for-postgresql/the-public-preview-of-polardb-for-postgresql-serverless-ends?spm=a2c4g.11186623.0.0.2e9f6cf0B4rIfC).
                    type: string
                  dbNodeCount:
                    description: |-
                      Number of the PolarDB cluster nodes, default is 2(Each cluster must contain at least a primary node and a read-only node). Add/remove nodes by modifying this parameter, valid values: [2~16].
                      -> NOTE: To avoid adding or removing multiple read-only nodes by mistake, the system allows you to add or remove one read-only node at a time.
                    type: number
                  dbNodeId:
                    description: 'The ID of the node or node subscript. Node subscript
                      values: 1 to 15.'
                    type: string
                  dbNodeNum:
                    description: 'The number of Standard Edition nodes. Default value:
                      1. Valid values are 1, 2. From version 1.235.0, Valid values
                      for PolarDB for MySQL Standard Edition: 1 to 8. Valid values
                      for PolarDB for MySQL Enterprise Edition: 1 to 16.'
                    type: number
                  dbType:
                    description: 'Database type. Value options: MySQL, Oracle, PostgreSQL.'
                    type: string
                  dbVersion:
                    description: Database version. Value options can refer to the
                      latest docs CreateDBCluster DBVersion.
                    type: string
                  defaultTimeZone:
                    description: |-
                      The time zone of the cluster. You can set the parameter to a value that is on the hour from -12:00 to +13:00 based on UTC. Example: 00:00. Default value: SYSTEM. This value indicates that the time zone of the cluster is the same as the time zone of the region.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: string
                  deletionLock:
                    description: |-
                      turn on table deletion_lock. Valid values are 0, 1. 1 means to open the cluster protection lock, 0 means to close the cluster protection lock
                      -> NOTE:  Cannot modify after created when pay_type is Prepaid .deletion_lock the cluster protection lock can be turned on or off when pay_type is Postpaid.
                    type: number
                  description:
                    description: The description of cluster.
                    type: string
                  encryptNewTables:
                    description: |-
                      turn on table auto encryption. Valid values are ON, OFF. Only MySQL 8.0 supports.
                      -> NOTE: encrypt_new_tables Polardb MySQL 8.0 cluster, after TDE and Automatic Encryption are enabled, all newly created tables are automatically encrypted in the cluster.
                    type: string
                  encryptionKey:
                    description: The ID of the custom key. encryption_key cannot be
                      modified after TDE is opened.
                    type: string
                  fromTimeService:
                    description: Immediate or scheduled kernel version upgrade. Valid
                      values are true, false. True means immediate execution, False
                      means scheduled execution.
                    type: string
                  gdnId:
                    description: |-
                      The ID of the global database network (GDN).
                      -> NOTE: This parameter is required if CreationOption is set to CreateGdnStandby.
                    type: string
                  hotReplicaMode:
                    description: Indicates whether the hot standby feature is enabled.
                      Valid values are ON, OFF. Only MySQL supports.
                    type: string
                  hotStandbyCluster:
                    description: Whether to enable the hot standby cluster. Valid
                      values are ON, OFF.
                    type: string
                  imciSwitch:
                    description: |-
                      Specifies whether to enable the In-Memory Column Index (IMCI) feature. Valid values are ON, OFF.
                      -> NOTE:  Only polardb MySQL Cluster version is available. The cluster with minor version number of 8.0.1 supports the column index feature, and the specific kernel version must be 8.0.1.1.22 or above.
                      -> NOTE:  The single node, the single node version of the history library, and the cluster version of the history library do not support column save indexes.
                    type: string
                  loosePolarLogBin:
                    description: |-
                      Enable the Binlog function. Default value: OFF. Valid values are OFF, ON.
                      -> NOTE: This parameter is valid only MySQL Engine supports.
                    type: string
                  looseXengine:
                    description: |-
                      Specifies whether to enable X-Engine. Valid values are ON, OFF.
                      -> NOTE: This parameter takes effect only if you do not set creation_option to CreateGdnStandby and you set db_type to MySQL and db_version to 8.0. To enable X-Engine on a node, make sure that the memory of the node is greater than or equal to 8 GB in size.
                    type: string
                  looseXengineUseMemoryPct:
                    description: |-
                      Set the ratio to enable the X-Engine storage engine. Valid values: 10 to 90.
                      -> NOTE: When the parameter loose_xengine is ON, loose_xengine_use_memory_pct takes effect.
                    type: number
                  lowerCaseTableNames:
                    description: |-
                      Specifies whether the table names are case-sensitive. Default value: 1.  Valid values are 1, 0.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: number
                  maintainTime:
                    description: 'Maintainable time period format of the instance:
                      HH:MMZ-HH:MMZ (UTC time)'
                    type: string
                  modifyType:
                    description: Use as db_node_class change class, define upgrade
                      or downgrade. Valid values are Upgrade, Downgrade, Default to
                      Upgrade.
                    type: string
                  parameterGroupId:
                    description: |-
                      The ID of the parameter template
                      -> NOTE: You can call the DescribeParameterGroups operation to query the details of all parameter templates of a specified region, such as the ID of a parameter template.
                    type: string
                  parameters:
                    description: Set of parameters needs to be set after DB cluster
                      was launched. Available parameters can refer to the latest docs
                      View database parameter templates .See parameters below.
                    items:
                      properties:
                        name:
                          description: Kernel parameter name.
                          type: string
                        value:
                          description: Kernel parameter value.
                          type: string
                      type: object
                    type: array
                  payType:
                    description: Valid values are PrePaid, PostPaid, Default to PostPaid.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy DB cluster (in month). It is valid when pay_type is PrePaid. Valid values: [1~9], 12, 24, 36.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  plannedEndTime:
                    description: |-
                      The latest time to start executing the target scheduled task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The latest time must be 30 minutes or more later than the start time. If PlannedStartTime is set but this parameter is not specified, the latest time to execute the target task defaults to the start time+30 minutes. For example, when the PlannedStartTime is set to 2021-01-14T09:00:00Z and this parameter is left blank, the target task will start executing at the latest on 2021-01-14T09:30:00Z.
                    type: string
                  plannedStartTime:
                    description: |-
                      The earliest time to start executing a scheduled (i.e. within the target time period) kernel version upgrade task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The starting time range is any time point within the next 24 hours. For example, the current time is 2021-01-14T09:00:00Z, and the allowed start time range for filling in here is 2021-01-14T09:00:00Z~2021-01-15T09:00:00Z. If this parameter is left blank, the kernel version upgrade task will be executed immediately by default.
                    type: string
                  provisionedIops:
                    description: |-
                      The provisioned read/write IOPS of the ESSD AutoPL disk. Valid values: 0 to min{50,000, 1,000 × Capacity - Baseline IOPS}. Baseline IOPS = min{1,800 + 50 × Capacity, 50,000}.
                      -> NOTE: This parameter is available only if the StorageType parameter is set to ESSDAUTOPL.
                    type: string
                  proxyClass:
                    description: |-
                      The specifications of the Standard Edition PolarProxy. Available parameters can refer to the latest docs CreateDBCluster
                      -> NOTE: This parameter is valid only for standard edition clusters.
                    type: string
                  proxyType:
                    description: |-
                      The type of PolarProxy. Valid values are EXCLUSIVE GENERAL.
                      -> NOTE: This parameter is valid for both standard and enterprise clusters.
                    type: string
                  renewalStatus:
                    description: Valid values are AutoRenewal, Normal, NotRenewal,
                      Default to NotRenewal.
                    type: string
                  resourceGroupId:
                    description: The ID of resource group which the PolarDB cluster
                      belongs. If not specified, then it belongs to the default resource
                      group.
                    type: string
                  roleArn:
                    description: The Alibaba Cloud Resource Name (ARN) of the RAM
                      role. A RAM role is a virtual identity that you can create within
                      your Alibaba Cloud account. For more information see RAM role
                      overview.
                    type: string
                  scaleApRoNumMax:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleApRoNumMin:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleMax:
                    description: 'The maximum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 32 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleMin:
                    description: 'The minimum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 31 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMax:
                    description: 'The maximum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMin:
                    description: 'The minimum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  secondsUntilAutoPause:
                    description: 'The detection period for No-activity Suspension.
                      Valid values: 300 to 86,4005. Unit: seconds. The detection duration
                      must be a multiple of 300 seconds. This parameter is valid only
                      for serverless clusters.'
                    type: number
                  securityGroupIds:
                    description: |-
                      The ID of the security group. Separate multiple security groups with commas (,). You can add a maximum of three security groups to a cluster.
                      -> NOTE: Because of data backup and migration, change DB cluster type and storage would cost 15~20 minutes. Please make full preparation before changing them.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityIps:
                    description: |-
                      This attribute has been deprecated from v1.130.0 and using db_cluster_ip_array sub-element security_ips instead.
                      Its value is same as db_cluster_ip_array sub-element security_ips value and its db_cluster_ip_array_name is "default".
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serverlessSteadySwitch:
                    description: |-
                      Serverless steady-state switch. Valid values are ON, OFF. This parameter is valid only for serverless clusters.
                      -> NOTE: When serverless_steady_switch is ON and serverless_type is SteadyServerless, parameters scale_min, scale_max, scale_ro_num_min and scale_ro_num_max are all required.
                    type: string
                  serverlessType:
                    description: The type of the serverless cluster. Valid values
                      AgileServerless, SteadyServerless. This parameter is valid only
                      for serverless clusters.
                    type: string
                  sourceResourceId:
                    description: The ID of the source RDS instance or the ID of the
                      source PolarDB cluster. This parameter is required only when
                      CreationOption is set to MigrationFromRDS, CloneFromRDS, or
                      CloneFromPolarDB.Value options can refer to the latest docs
                      CreateDBCluster SourceResourceId.
                    type: string
                  storagePayType:
                    description: The billing method of the storage. Valid values Postpaid,
                      Prepaid.
                    type: string
                  storageSpace:
                    description: |-
                      Storage space charged by space (monthly package). Unit: GB.
                      -> NOTE:  Valid values for PolarDB for MySQL Standard Edition: 20 to 32000. It is valid when pay_type are PrePaid ,PostPaid.
                      -> NOTE:  Valid values for PolarDB for MySQL Enterprise Edition: 50 to 100000.It is valid when pay_type is PrePaid.
                    type: number
                  storageType:
                    description: The storage type of the cluster. Enterprise storage
                      type values are PSL5, PSL4. The standard version storage type
                      values are ESSDPL1, ESSDPL2, ESSDPL3, ESSDPL0, ESSDAUTOPL. The
                      standard version only supports MySQL and PostgreSQL.
                    type: string
                  strictConsistency:
                    description: Whether the cluster has enabled strong data consistency
                      across multiple zones. Valid values are ON, OFF. Available parameters
                      can refer to the latest docs CreateDBCluster
                    type: string
                  subCategory:
                    description: The category of the cluster. Valid values are Exclusive,
                      General. Only MySQL supports.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  targetDbRevisionVersionCode:
                    description: The Version Code of the target version, whose parameter
                      values can be obtained from the DescribeDBClusterVersion interface.
                    type: string
                  tdeStatus:
                    description: |-
                      turn on TDE encryption. Valid values are Enabled, Disabled. Default to Disabled. TDE cannot be closed after it is turned on.
                      -> NOTE: tde_status Cannot modify after created when db_type is PostgreSQL or Oracle.tde_status only support modification from Disabled to Enabled when db_type is MySQL.
                    type: string
                  upgradeType:
                    description: Version upgrade type. Valid values are PROXY, DB,
                      ALL. PROXY means upgrading the proxy version, DB means upgrading
                      the db version, ALL means upgrading both db and proxy versions
                      simultaneously.
                    type: string
                  vpcId:
                    description: The id of the VPC.
                    type: string
                  vswitchId:
                    description: |-
                      The virtual switch ID to launch DB instances in one VPC.
                      -> NOTE: If vswitch_id is not specified, system will get a vswitch belongs to the user automatically.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneId:
                    description: The Zone to launch the DB cluster. it supports multiple
                      zone.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowShutDown:
                    description: 'Specifies whether to enable the no-activity suspension
                      feature. Default value: false. Valid values are true, false.
                      This parameter is valid only for serverless clusters.'
                    type: string
                  autoRenewPeriod:
                    description: Auto-renewal period of an cluster, in the unit of
                      the month. It is valid when pay_type is PrePaid. Valid value:1,
                      2, 3, 6, 12, 24, 36, Default to 1.
                    type: number
                  backupRetentionPolicyOnClusterDeletion:
                    description: The retention policy for the backup sets when you
                      delete the cluster.  Valid values are ALL, LATEST, NONE. Value
                      options can refer to the latest docs DeleteDBCluster
                    type: string
                  cloneDataPoint:
                    description: |-
                      The time point of data to be cloned. Valid values are LATEST,BackupID,Timestamp.Value options can refer to the latest docs CreateDBCluster CloneDataPoint.
                      -> NOTE: If CreationOption is set to CloneFromRDS, the value of this parameter must be LATEST.
                    type: string
                  collectorStatus:
                    description: Specifies whether to enable or disable SQL data collector.
                      Valid values are Enable, Disabled.
                    type: string
                  compressStorage:
                    description: |-
                      Enable storage compression function. The value of this parameter is ON. Only MySQL supports.
                      -> NOTE: When the value of db_type is not MySQL, the value of creation_option is neither empty nor Normal, and the value of storage_type is not PSL4, this field will be ignored.
                    type: string
                  creationCategory:
                    description: |-
                      The edition of the PolarDB service. Valid values are Normal,Basic,ArchiveNormal,NormalMultimaster,SENormal.Value options can refer to the latest docs CreateDBCluster CreationCategory.
                      -> NOTE: You can set this parameter to Basic only when DBType is set to MySQL and DBVersion is set to 5.6, 5.7, or 8.0. You can set this parameter to Archive only when DBType is set to MySQL and DBVersion is set to 8.0. From version 1.188.0, creation_category can be set to NormalMultimaster. From version 1.203.0, creation_category can be set to SENormal.
                    type: string
                  creationOption:
                    description: 'The method that is used to create a cluster. Valid
                      values are Normal,CloneFromPolarDB,CloneFromRDS,MigrationFromRDS,CreateGdnStandby,RecoverFromRecyclebin.
                      NOTE: From version 1.233.0, creation_option can be set to RecoverFromRecyclebin.
                      Value options can refer to the latest docs CreateDBCluster CreationOption.'
                    type: string
                  dbClusterIpArray:
                    description: db_cluster_ip_array defines how users can send requests
                      to your API. See db_cluster_ip_array below.
                    items:
                      properties:
                        dbClusterIpArrayName:
                          description: |-
                            The name of the IP whitelist group. The group name must be 2 to 120 characters in length and consists of lowercase letters and digits. It must start with a letter, and end with a letter or a digit.
                            NOTE: If the specified whitelist group name does not exist, the whitelist group is created. If the specified whitelist group name exists, the whitelist group is modified. If you do not specify this parameter, the default group is modified. You can create a maximum of 50 IP whitelist groups for a cluster.
                          type: string
                        modifyMode:
                          description: |-
                            The method for modifying the IP whitelist. Valid values are Cover, Append, Delete.
                            NOTE: There does not recommend setting modify_mode to Append or Delete and it will bring a potential diff error.
                          type: string
                        securityIps:
                          description: List of IP addresses allowed to access all
                            databases of a cluster. The list contains up to 1,000
                            IP addresses, separated by commas. Supported formats include
                            0.0.0.0/0, 10.23.12.24 (IP), and 10.23.12.24/24 (Classless
                            Inter-Domain Routing (CIDR) mode. /24 represents the length
                            of the prefix in an IP address. The range of the prefix
                            length is [1,32]).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  dbMinorVersion:
                    description: Database minor version. Value options can refer to
                      the latest docs CreateDBCluster DBMinorVersion. This parameter
                      takes effect only when db_type is MySQL and db_version is 8.0.
                    type: string
                  dbNodeClass:
                    description: |-
                      The db_node_class of cluster node.
                      -> NOTE: Node specifications are divided into cluster version, single node version and History Library version. They can't change each other, but the general specification and exclusive specification of cluster version can be changed.
                      From version 1.204.0, If you need to create a Serverless cluster with MySQL , db_node_class can be set to polar.mysql.sl.small.
                      From version 1.229.1, If you need to create a Serverless cluster with PostgreSQL 14 using the SENormal edition, db_node_class can be set to polar.pg.sl.small.c(x86 Architecture). Region can refer to the latest docs(https://help.aliyun.com/zh/polardb/polardb-for-postgresql/the-public-preview-of-polardb-for-postgresql-serverless-ends?spm=a2c4g.11186623.0.0.2e9f6cf0B4rIfC).
                    type: string
                  dbNodeCount:
                    description: |-
                      Number of the PolarDB cluster nodes, default is 2(Each cluster must contain at least a primary node and a read-only node). Add/remove nodes by modifying this parameter, valid values: [2~16].
                      -> NOTE: To avoid adding or removing multiple read-only nodes by mistake, the system allows you to add or remove one read-only node at a time.
                    type: number
                  dbNodeId:
                    description: 'The ID of the node or node subscript. Node subscript
                      values: 1 to 15.'
                    type: string
                  dbNodeNum:
                    description: 'The number of Standard Edition nodes. Default value:
                      1. Valid values are 1, 2. From version 1.235.0, Valid values
                      for PolarDB for MySQL Standard Edition: 1 to 8. Valid values
                      for PolarDB for MySQL Enterprise Edition: 1 to 16.'
                    type: number
                  dbType:
                    description: 'Database type. Value options: MySQL, Oracle, PostgreSQL.'
                    type: string
                  dbVersion:
                    description: Database version. Value options can refer to the
                      latest docs CreateDBCluster DBVersion.
                    type: string
                  defaultTimeZone:
                    description: |-
                      The time zone of the cluster. You can set the parameter to a value that is on the hour from -12:00 to +13:00 based on UTC. Example: 00:00. Default value: SYSTEM. This value indicates that the time zone of the cluster is the same as the time zone of the region.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: string
                  deletionLock:
                    description: |-
                      turn on table deletion_lock. Valid values are 0, 1. 1 means to open the cluster protection lock, 0 means to close the cluster protection lock
                      -> NOTE:  Cannot modify after created when pay_type is Prepaid .deletion_lock the cluster protection lock can be turned on or off when pay_type is Postpaid.
                    type: number
                  description:
                    description: The description of cluster.
                    type: string
                  encryptNewTables:
                    description: |-
                      turn on table auto encryption. Valid values are ON, OFF. Only MySQL 8.0 supports.
                      -> NOTE: encrypt_new_tables Polardb MySQL 8.0 cluster, after TDE and Automatic Encryption are enabled, all newly created tables are automatically encrypted in the cluster.
                    type: string
                  encryptionKey:
                    description: The ID of the custom key. encryption_key cannot be
                      modified after TDE is opened.
                    type: string
                  fromTimeService:
                    description: Immediate or scheduled kernel version upgrade. Valid
                      values are true, false. True means immediate execution, False
                      means scheduled execution.
                    type: string
                  gdnId:
                    description: |-
                      The ID of the global database network (GDN).
                      -> NOTE: This parameter is required if CreationOption is set to CreateGdnStandby.
                    type: string
                  hotReplicaMode:
                    description: Indicates whether the hot standby feature is enabled.
                      Valid values are ON, OFF. Only MySQL supports.
                    type: string
                  hotStandbyCluster:
                    description: Whether to enable the hot standby cluster. Valid
                      values are ON, OFF.
                    type: string
                  imciSwitch:
                    description: |-
                      Specifies whether to enable the In-Memory Column Index (IMCI) feature. Valid values are ON, OFF.
                      -> NOTE:  Only polardb MySQL Cluster version is available. The cluster with minor version number of 8.0.1 supports the column index feature, and the specific kernel version must be 8.0.1.1.22 or above.
                      -> NOTE:  The single node, the single node version of the history library, and the cluster version of the history library do not support column save indexes.
                    type: string
                  loosePolarLogBin:
                    description: |-
                      Enable the Binlog function. Default value: OFF. Valid values are OFF, ON.
                      -> NOTE: This parameter is valid only MySQL Engine supports.
                    type: string
                  looseXengine:
                    description: |-
                      Specifies whether to enable X-Engine. Valid values are ON, OFF.
                      -> NOTE: This parameter takes effect only if you do not set creation_option to CreateGdnStandby and you set db_type to MySQL and db_version to 8.0. To enable X-Engine on a node, make sure that the memory of the node is greater than or equal to 8 GB in size.
                    type: string
                  looseXengineUseMemoryPct:
                    description: |-
                      Set the ratio to enable the X-Engine storage engine. Valid values: 10 to 90.
                      -> NOTE: When the parameter loose_xengine is ON, loose_xengine_use_memory_pct takes effect.
                    type: number
                  lowerCaseTableNames:
                    description: |-
                      Specifies whether the table names are case-sensitive. Default value: 1.  Valid values are 1, 0.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: number
                  maintainTime:
                    description: 'Maintainable time period format of the instance:
                      HH:MMZ-HH:MMZ (UTC time)'
                    type: string
                  modifyType:
                    description: Use as db_node_class change class, define upgrade
                      or downgrade. Valid values are Upgrade, Downgrade, Default to
                      Upgrade.
                    type: string
                  parameterGroupId:
                    description: |-
                      The ID of the parameter template
                      -> NOTE: You can call the DescribeParameterGroups operation to query the details of all parameter templates of a specified region, such as the ID of a parameter template.
                    type: string
                  parameters:
                    description: Set of parameters needs to be set after DB cluster
                      was launched. Available parameters can refer to the latest docs
                      View database parameter templates .See parameters below.
                    items:
                      properties:
                        name:
                          description: Kernel parameter name.
                          type: string
                        value:
                          description: Kernel parameter value.
                          type: string
                      type: object
                    type: array
                  payType:
                    description: Valid values are PrePaid, PostPaid, Default to PostPaid.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy DB cluster (in month). It is valid when pay_type is PrePaid. Valid values: [1~9], 12, 24, 36.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  plannedEndTime:
                    description: |-
                      The latest time to start executing the target scheduled task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The latest time must be 30 minutes or more later than the start time. If PlannedStartTime is set but this parameter is not specified, the latest time to execute the target task defaults to the start time+30 minutes. For example, when the PlannedStartTime is set to 2021-01-14T09:00:00Z and this parameter is left blank, the target task will start executing at the latest on 2021-01-14T09:30:00Z.
                    type: string
                  plannedStartTime:
                    description: |-
                      The earliest time to start executing a scheduled (i.e. within the target time period) kernel version upgrade task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The starting time range is any time point within the next 24 hours. For example, the current time is 2021-01-14T09:00:00Z, and the allowed start time range for filling in here is 2021-01-14T09:00:00Z~2021-01-15T09:00:00Z. If this parameter is left blank, the kernel version upgrade task will be executed immediately by default.
                    type: string
                  provisionedIops:
                    description: |-
                      The provisioned read/write IOPS of the ESSD AutoPL disk. Valid values: 0 to min{50,000, 1,000 × Capacity - Baseline IOPS}. Baseline IOPS = min{1,800 + 50 × Capacity, 50,000}.
                      -> NOTE: This parameter is available only if the StorageType parameter is set to ESSDAUTOPL.
                    type: string
                  proxyClass:
                    description: |-
                      The specifications of the Standard Edition PolarProxy. Available parameters can refer to the latest docs CreateDBCluster
                      -> NOTE: This parameter is valid only for standard edition clusters.
                    type: string
                  proxyType:
                    description: |-
                      The type of PolarProxy. Valid values are EXCLUSIVE GENERAL.
                      -> NOTE: This parameter is valid for both standard and enterprise clusters.
                    type: string
                  renewalStatus:
                    description: Valid values are AutoRenewal, Normal, NotRenewal,
                      Default to NotRenewal.
                    type: string
                  resourceGroupId:
                    description: The ID of resource group which the PolarDB cluster
                      belongs. If not specified, then it belongs to the default resource
                      group.
                    type: string
                  roleArn:
                    description: The Alibaba Cloud Resource Name (ARN) of the RAM
                      role. A RAM role is a virtual identity that you can create within
                      your Alibaba Cloud account. For more information see RAM role
                      overview.
                    type: string
                  scaleApRoNumMax:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleApRoNumMin:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleMax:
                    description: 'The maximum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 32 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleMin:
                    description: 'The minimum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 31 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMax:
                    description: 'The maximum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMin:
                    description: 'The minimum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  secondsUntilAutoPause:
                    description: 'The detection period for No-activity Suspension.
                      Valid values: 300 to 86,4005. Unit: seconds. The detection duration
                      must be a multiple of 300 seconds. This parameter is valid only
                      for serverless clusters.'
                    type: number
                  securityGroupIds:
                    description: |-
                      The ID of the security group. Separate multiple security groups with commas (,). You can add a maximum of three security groups to a cluster.
                      -> NOTE: Because of data backup and migration, change DB cluster type and storage would cost 15~20 minutes. Please make full preparation before changing them.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityIps:
                    description: |-
                      This attribute has been deprecated from v1.130.0 and using db_cluster_ip_array sub-element security_ips instead.
                      Its value is same as db_cluster_ip_array sub-element security_ips value and its db_cluster_ip_array_name is "default".
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serverlessSteadySwitch:
                    description: |-
                      Serverless steady-state switch. Valid values are ON, OFF. This parameter is valid only for serverless clusters.
                      -> NOTE: When serverless_steady_switch is ON and serverless_type is SteadyServerless, parameters scale_min, scale_max, scale_ro_num_min and scale_ro_num_max are all required.
                    type: string
                  serverlessType:
                    description: The type of the serverless cluster. Valid values
                      AgileServerless, SteadyServerless. This parameter is valid only
                      for serverless clusters.
                    type: string
                  sourceResourceId:
                    description: The ID of the source RDS instance or the ID of the
                      source PolarDB cluster. This parameter is required only when
                      CreationOption is set to MigrationFromRDS, CloneFromRDS, or
                      CloneFromPolarDB.Value options can refer to the latest docs
                      CreateDBCluster SourceResourceId.
                    type: string
                  storagePayType:
                    description: The billing method of the storage. Valid values Postpaid,
                      Prepaid.
                    type: string
                  storageSpace:
                    description: |-
                      Storage space charged by space (monthly package). Unit: GB.
                      -> NOTE:  Valid values for PolarDB for MySQL Standard Edition: 20 to 32000. It is valid when pay_type are PrePaid ,PostPaid.
                      -> NOTE:  Valid values for PolarDB for MySQL Enterprise Edition: 50 to 100000.It is valid when pay_type is PrePaid.
                    type: number
                  storageType:
                    description: The storage type of the cluster. Enterprise storage
                      type values are PSL5, PSL4. The standard version storage type
                      values are ESSDPL1, ESSDPL2, ESSDPL3, ESSDPL0, ESSDAUTOPL. The
                      standard version only supports MySQL and PostgreSQL.
                    type: string
                  strictConsistency:
                    description: Whether the cluster has enabled strong data consistency
                      across multiple zones. Valid values are ON, OFF. Available parameters
                      can refer to the latest docs CreateDBCluster
                    type: string
                  subCategory:
                    description: The category of the cluster. Valid values are Exclusive,
                      General. Only MySQL supports.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  targetDbRevisionVersionCode:
                    description: The Version Code of the target version, whose parameter
                      values can be obtained from the DescribeDBClusterVersion interface.
                    type: string
                  tdeStatus:
                    description: |-
                      turn on TDE encryption. Valid values are Enabled, Disabled. Default to Disabled. TDE cannot be closed after it is turned on.
                      -> NOTE: tde_status Cannot modify after created when db_type is PostgreSQL or Oracle.tde_status only support modification from Disabled to Enabled when db_type is MySQL.
                    type: string
                  upgradeType:
                    description: Version upgrade type. Valid values are PROXY, DB,
                      ALL. PROXY means upgrading the proxy version, DB means upgrading
                      the db version, ALL means upgrading both db and proxy versions
                      simultaneously.
                    type: string
                  vpcId:
                    description: The id of the VPC.
                    type: string
                  vswitchId:
                    description: |-
                      The virtual switch ID to launch DB instances in one VPC.
                      -> NOTE: If vswitch_id is not specified, system will get a vswitch belongs to the user automatically.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneId:
                    description: The Zone to launch the DB cluster. it supports multiple
                      zone.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.dbNodeClass is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dbNodeClass)
                || (has(self.initProvider) && has(self.initProvider.dbNodeClass))'
            - message: spec.forProvider.dbType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dbType)
                || (has(self.initProvider) && has(self.initProvider.dbType))'
            - message: spec.forProvider.dbVersion is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.dbVersion)
                || (has(self.initProvider) && has(self.initProvider.dbVersion))'
          status:
            description: ClusterStatus defines the observed state of Cluster.
            properties:
              atProvider:
                properties:
                  allowShutDown:
                    description: 'Specifies whether to enable the no-activity suspension
                      feature. Default value: false. Valid values are true, false.
                      This parameter is valid only for serverless clusters.'
                    type: string
                  autoRenewPeriod:
                    description: Auto-renewal period of an cluster, in the unit of
                      the month. It is valid when pay_type is PrePaid. Valid value:1,
                      2, 3, 6, 12, 24, 36, Default to 1.
                    type: number
                  backupRetentionPolicyOnClusterDeletion:
                    description: The retention policy for the backup sets when you
                      delete the cluster.  Valid values are ALL, LATEST, NONE. Value
                      options can refer to the latest docs DeleteDBCluster
                    type: string
                  cloneDataPoint:
                    description: |-
                      The time point of data to be cloned. Valid values are LATEST,BackupID,Timestamp.Value options can refer to the latest docs CreateDBCluster CloneDataPoint.
                      -> NOTE: If CreationOption is set to CloneFromRDS, the value of this parameter must be LATEST.
                    type: string
                  collectorStatus:
                    description: Specifies whether to enable or disable SQL data collector.
                      Valid values are Enable, Disabled.
                    type: string
                  compressStorage:
                    description: |-
                      Enable storage compression function. The value of this parameter is ON. Only MySQL supports.
                      -> NOTE: When the value of db_type is not MySQL, the value of creation_option is neither empty nor Normal, and the value of storage_type is not PSL4, this field will be ignored.
                    type: string
                  connectionString:
                    description: (Available since 1.81.0) PolarDB cluster connection
                      string.
                    type: string
                  createTime:
                    description: (Available since 1.204.1) PolarDB cluster creation
                      time.
                    type: string
                  creationCategory:
                    description: |-
                      The edition of the PolarDB service. Valid values are Normal,Basic,ArchiveNormal,NormalMultimaster,SENormal.Value options can refer to the latest docs CreateDBCluster CreationCategory.
                      -> NOTE: You can set this parameter to Basic only when DBType is set to MySQL and DBVersion is set to 5.6, 5.7, or 8.0. You can set this parameter to Archive only when DBType is set to MySQL and DBVersion is set to 8.0. From version 1.188.0, creation_category can be set to NormalMultimaster. From version 1.203.0, creation_category can be set to SENormal.
                    type: string
                  creationOption:
                    description: 'The method that is used to create a cluster. Valid
                      values are Normal,CloneFromPolarDB,CloneFromRDS,MigrationFromRDS,CreateGdnStandby,RecoverFromRecyclebin.
                      NOTE: From version 1.233.0, creation_option can be set to RecoverFromRecyclebin.
                      Value options can refer to the latest docs CreateDBCluster CreationOption.'
                    type: string
                  dbClusterIpArray:
                    description: db_cluster_ip_array defines how users can send requests
                      to your API. See db_cluster_ip_array below.
                    items:
                      properties:
                        dbClusterIpArrayName:
                          description: |-
                            The name of the IP whitelist group. The group name must be 2 to 120 characters in length and consists of lowercase letters and digits. It must start with a letter, and end with a letter or a digit.
                            NOTE: If the specified whitelist group name does not exist, the whitelist group is created. If the specified whitelist group name exists, the whitelist group is modified. If you do not specify this parameter, the default group is modified. You can create a maximum of 50 IP whitelist groups for a cluster.
                          type: string
                        modifyMode:
                          description: |-
                            The method for modifying the IP whitelist. Valid values are Cover, Append, Delete.
                            NOTE: There does not recommend setting modify_mode to Append or Delete and it will bring a potential diff error.
                          type: string
                        securityIps:
                          description: List of IP addresses allowed to access all
                            databases of a cluster. The list contains up to 1,000
                            IP addresses, separated by commas. Supported formats include
                            0.0.0.0/0, 10.23.12.24 (IP), and 10.23.12.24/24 (Classless
                            Inter-Domain Routing (CIDR) mode. /24 represents the length
                            of the prefix in an IP address. The range of the prefix
                            length is [1,32]).
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                  dbMinorVersion:
                    description: Database minor version. Value options can refer to
                      the latest docs CreateDBCluster DBMinorVersion. This parameter
                      takes effect only when db_type is MySQL and db_version is 8.0.
                    type: string
                  dbNodeClass:
                    description: |-
                      The db_node_class of cluster node.
                      -> NOTE: Node specifications are divided into cluster version, single node version and History Library version. They can't change each other, but the general specification and exclusive specification of cluster version can be changed.
                      From version 1.204.0, If you need to create a Serverless cluster with MySQL , db_node_class can be set to polar.mysql.sl.small.
                      From version 1.229.1, If you need to create a Serverless cluster with PostgreSQL 14 using the SENormal edition, db_node_class can be set to polar.pg.sl.small.c(x86 Architecture). Region can refer to the latest docs(https://help.aliyun.com/zh/polardb/polardb-for-postgresql/the-public-preview-of-polardb-for-postgresql-serverless-ends?spm=a2c4g.11186623.0.0.2e9f6cf0B4rIfC).
                    type: string
                  dbNodeCount:
                    description: |-
                      Number of the PolarDB cluster nodes, default is 2(Each cluster must contain at least a primary node and a read-only node). Add/remove nodes by modifying this parameter, valid values: [2~16].
                      -> NOTE: To avoid adding or removing multiple read-only nodes by mistake, the system allows you to add or remove one read-only node at a time.
                    type: number
                  dbNodeId:
                    description: 'The ID of the node or node subscript. Node subscript
                      values: 1 to 15.'
                    type: string
                  dbNodeNum:
                    description: 'The number of Standard Edition nodes. Default value:
                      1. Valid values are 1, 2. From version 1.235.0, Valid values
                      for PolarDB for MySQL Standard Edition: 1 to 8. Valid values
                      for PolarDB for MySQL Enterprise Edition: 1 to 16.'
                    type: number
                  dbRevisionVersionList:
                    description: '(Available since v1.216.0) The db_revision_version_list
                      supports the following:'
                    items:
                      properties:
                        releaseNote:
                          description: (Available since v1.216.0) The revised version
                            Code of the database engine is used to specify the upgrade
                            to the target version.
                          type: string
                        releaseType:
                          description: (Available since v1.216.0) Database version
                            release status. Valid values are Stable, Old, HighRisk.
                          type: string
                        revisionVersionCode:
                          description: (Available since v1.216.0) The revised version
                            Code of the database engine is used to specify the upgrade
                            to the target version.
                          type: string
                        revisionVersionName:
                          description: (Available since v1.216.0) The revision version
                            number of the database engine.
                          type: string
                      type: object
                    type: array
                  dbType:
                    description: 'Database type. Value options: MySQL, Oracle, PostgreSQL.'
                    type: string
                  dbVersion:
                    description: Database version. Value options can refer to the
                      latest docs CreateDBCluster DBVersion.
                    type: string
                  defaultTimeZone:
                    description: |-
                      The time zone of the cluster. You can set the parameter to a value that is on the hour from -12:00 to +13:00 based on UTC. Example: 00:00. Default value: SYSTEM. This value indicates that the time zone of the cluster is the same as the time zone of the region.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: string
                  deletionLock:
                    description: |-
                      turn on table deletion_lock. Valid values are 0, 1. 1 means to open the cluster protection lock, 0 means to close the cluster protection lock
                      -> NOTE:  Cannot modify after created when pay_type is Prepaid .deletion_lock the cluster protection lock can be turned on or off when pay_type is Postpaid.
                    type: number
                  description:
                    description: The description of cluster.
                    type: string
                  encryptNewTables:
                    description: |-
                      turn on table auto encryption. Valid values are ON, OFF. Only MySQL 8.0 supports.
                      -> NOTE: encrypt_new_tables Polardb MySQL 8.0 cluster, after TDE and Automatic Encryption are enabled, all newly created tables are automatically encrypted in the cluster.
                    type: string
                  encryptionKey:
                    description: The ID of the custom key. encryption_key cannot be
                      modified after TDE is opened.
                    type: string
                  fromTimeService:
                    description: Immediate or scheduled kernel version upgrade. Valid
                      values are true, false. True means immediate execution, False
                      means scheduled execution.
                    type: string
                  gdnId:
                    description: |-
                      The ID of the global database network (GDN).
                      -> NOTE: This parameter is required if CreationOption is set to CreateGdnStandby.
                    type: string
                  hotReplicaMode:
                    description: Indicates whether the hot standby feature is enabled.
                      Valid values are ON, OFF. Only MySQL supports.
                    type: string
                  hotStandbyCluster:
                    description: Whether to enable the hot standby cluster. Valid
                      values are ON, OFF.
                    type: string
                  id:
                    description: The PolarDB cluster ID.
                    type: string
                  imciSwitch:
                    description: |-
                      Specifies whether to enable the In-Memory Column Index (IMCI) feature. Valid values are ON, OFF.
                      -> NOTE:  Only polardb MySQL Cluster version is available. The cluster with minor version number of 8.0.1 supports the column index feature, and the specific kernel version must be 8.0.1.1.22 or above.
                      -> NOTE:  The single node, the single node version of the history library, and the cluster version of the history library do not support column save indexes.
                    type: string
                  loosePolarLogBin:
                    description: |-
                      Enable the Binlog function. Default value: OFF. Valid values are OFF, ON.
                      -> NOTE: This parameter is valid only MySQL Engine supports.
                    type: string
                  looseXengine:
                    description: |-
                      Specifies whether to enable X-Engine. Valid values are ON, OFF.
                      -> NOTE: This parameter takes effect only if you do not set creation_option to CreateGdnStandby and you set db_type to MySQL and db_version to 8.0. To enable X-Engine on a node, make sure that the memory of the node is greater than or equal to 8 GB in size.
                    type: string
                  looseXengineUseMemoryPct:
                    description: |-
                      Set the ratio to enable the X-Engine storage engine. Valid values: 10 to 90.
                      -> NOTE: When the parameter loose_xengine is ON, loose_xengine_use_memory_pct takes effect.
                    type: number
                  lowerCaseTableNames:
                    description: |-
                      Specifies whether the table names are case-sensitive. Default value: 1.  Valid values are 1, 0.
                      -> NOTE: This parameter is valid only when the DBType parameter is set to MySQL.
                    type: number
                  maintainTime:
                    description: 'Maintainable time period format of the instance:
                      HH:MMZ-HH:MMZ (UTC time)'
                    type: string
                  modifyType:
                    description: Use as db_node_class change class, define upgrade
                      or downgrade. Valid values are Upgrade, Downgrade, Default to
                      Upgrade.
                    type: string
                  parameterGroupId:
                    description: |-
                      The ID of the parameter template
                      -> NOTE: You can call the DescribeParameterGroups operation to query the details of all parameter templates of a specified region, such as the ID of a parameter template.
                    type: string
                  parameters:
                    description: Set of parameters needs to be set after DB cluster
                      was launched. Available parameters can refer to the latest docs
                      View database parameter templates .See parameters below.
                    items:
                      properties:
                        name:
                          description: Kernel parameter name.
                          type: string
                        value:
                          description: Kernel parameter value.
                          type: string
                      type: object
                    type: array
                  payType:
                    description: Valid values are PrePaid, PostPaid, Default to PostPaid.
                    type: string
                  period:
                    description: |-
                      The duration that you will buy DB cluster (in month). It is valid when pay_type is PrePaid. Valid values: [1~9], 12, 24, 36.
                      -> NOTE: The attribute period is only used to create Subscription instance or modify the PayAsYouGo instance to Subscription.
                    type: number
                  plannedEndTime:
                    description: |-
                      The latest time to start executing the target scheduled task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The latest time must be 30 minutes or more later than the start time. If PlannedStartTime is set but this parameter is not specified, the latest time to execute the target task defaults to the start time+30 minutes. For example, when the PlannedStartTime is set to 2021-01-14T09:00:00Z and this parameter is left blank, the target task will start executing at the latest on 2021-01-14T09:30:00Z.
                    type: string
                  plannedStartTime:
                    description: |-
                      The earliest time to start executing a scheduled (i.e. within the target time period) kernel version upgrade task. The format is YYYY-MM-DDThh: mm: ssZ (UTC).
                      -> NOTE: The starting time range is any time point within the next 24 hours. For example, the current time is 2021-01-14T09:00:00Z, and the allowed start time range for filling in here is 2021-01-14T09:00:00Z~2021-01-15T09:00:00Z. If this parameter is left blank, the kernel version upgrade task will be executed immediately by default.
                    type: string
                  port:
                    description: (Available since 1.196.0) PolarDB cluster connection
                      port.
                    type: string
                  provisionedIops:
                    description: |-
                      The provisioned read/write IOPS of the ESSD AutoPL disk. Valid values: 0 to min{50,000, 1,000 × Capacity - Baseline IOPS}. Baseline IOPS = min{1,800 + 50 × Capacity, 50,000}.
                      -> NOTE: This parameter is available only if the StorageType parameter is set to ESSDAUTOPL.
                    type: string
                  proxyClass:
                    description: |-
                      The specifications of the Standard Edition PolarProxy. Available parameters can refer to the latest docs CreateDBCluster
                      -> NOTE: This parameter is valid only for standard edition clusters.
                    type: string
                  proxyType:
                    description: |-
                      The type of PolarProxy. Valid values are EXCLUSIVE GENERAL.
                      -> NOTE: This parameter is valid for both standard and enterprise clusters.
                    type: string
                  renewalStatus:
                    description: Valid values are AutoRenewal, Normal, NotRenewal,
                      Default to NotRenewal.
                    type: string
                  resourceGroupId:
                    description: The ID of resource group which the PolarDB cluster
                      belongs. If not specified, then it belongs to the default resource
                      group.
                    type: string
                  roleArn:
                    description: The Alibaba Cloud Resource Name (ARN) of the RAM
                      role. A RAM role is a virtual identity that you can create within
                      your Alibaba Cloud account. For more information see RAM role
                      overview.
                    type: string
                  scaleApRoNumMax:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleApRoNumMin:
                    description: 'Number of Read-only Columnar Nodes. Valid values:
                      0 to 7. This parameter is valid only for serverless clusters.
                      This parameter is required when there are column nodes that
                      support steady-state serverless.'
                    type: number
                  scaleMax:
                    description: 'The maximum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 32 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleMin:
                    description: 'The minimum number of PCUs per node for scaling.
                      Valid values: 1 PCU to 31 PCUs when serverless_type is AgileServerless
                      and 0 PCU to 8 PCUs when serverless_type is SteadyServerless.
                      This parameter is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMax:
                    description: 'The maximum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  scaleRoNumMin:
                    description: 'The minimum number of read-only nodes for scaling.
                      Valid values: 0 to 15 when serverless_type is AgileServerless
                      and 0 to 7 when serverless_type is SteadyServerless. This parameter
                      is valid only for serverless clusters.'
                    type: number
                  secondsUntilAutoPause:
                    description: 'The detection period for No-activity Suspension.
                      Valid values: 300 to 86,4005. Unit: seconds. The detection duration
                      must be a multiple of 300 seconds. This parameter is valid only
                      for serverless clusters.'
                    type: number
                  securityGroupIds:
                    description: |-
                      The ID of the security group. Separate multiple security groups with commas (,). You can add a maximum of three security groups to a cluster.
                      -> NOTE: Because of data backup and migration, change DB cluster type and storage would cost 15~20 minutes. Please make full preparation before changing them.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityIps:
                    description: |-
                      This attribute has been deprecated from v1.130.0 and using db_cluster_ip_array sub-element security_ips instead.
                      Its value is same as db_cluster_ip_array sub-element security_ips value and its db_cluster_ip_array_name is "default".
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serverlessSteadySwitch:
                    description: |-
                      Serverless steady-state switch. Valid values are ON, OFF. This parameter is valid only for serverless clusters.
                      -> NOTE: When serverless_steady_switch is ON and serverless_type is SteadyServerless, parameters scale_min, scale_max, scale_ro_num_min and scale_ro_num_max are all required.
                    type: string
                  serverlessType:
                    description: The type of the serverless cluster. Valid values
                      AgileServerless, SteadyServerless. This parameter is valid only
                      for serverless clusters.
                    type: string
                  sourceResourceId:
                    description: The ID of the source RDS instance or the ID of the
                      source PolarDB cluster. This parameter is required only when
                      CreationOption is set to MigrationFromRDS, CloneFromRDS, or
                      CloneFromPolarDB.Value options can refer to the latest docs
                      CreateDBCluster SourceResourceId.
                    type: string
                  status:
                    description: (Available since 1.204.1) PolarDB cluster status.
                    type: string
                  storagePayType:
                    description: The billing method of the storage. Valid values Postpaid,
                      Prepaid.
                    type: string
                  storageSpace:
                    description: |-
                      Storage space charged by space (monthly package). Unit: GB.
                      -> NOTE:  Valid values for PolarDB for MySQL Standard Edition: 20 to 32000. It is valid when pay_type are PrePaid ,PostPaid.
                      -> NOTE:  Valid values for PolarDB for MySQL Enterprise Edition: 50 to 100000.It is valid when pay_type is PrePaid.
                    type: number
                  storageType:
                    description: The storage type of the cluster. Enterprise storage
                      type values are PSL5, PSL4. The standard version storage type
                      values are ESSDPL1, ESSDPL2, ESSDPL3, ESSDPL0, ESSDAUTOPL. The
                      standard version only supports MySQL and PostgreSQL.
                    type: string
                  strictConsistency:
                    description: Whether the cluster has enabled strong data consistency
                      across multiple zones. Valid values are ON, OFF. Available parameters
                      can refer to the latest docs CreateDBCluster
                    type: string
                  subCategory:
                    description: The category of the cluster. Valid values are Exclusive,
                      General. Only MySQL supports.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: A mapping of tags to assign to the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  targetDbRevisionVersionCode:
                    description: The Version Code of the target version, whose parameter
                      values can be obtained from the DescribeDBClusterVersion interface.
                    type: string
                  tdeRegion:
                    description: |-
                      (Available since 1.200.0) The region where the TDE key resides.
                      -> NOTE: TDE can be enabled on clusters that have joined a global database network (GDN). After TDE is enabled on the primary cluster in a GDN, TDE is enabled on the secondary clusters in the GDN by default. The key used by the secondary clusters and the region for the key resides must be the same as the primary cluster. The region of the key cannot be modified.
                      -> NOTE: You cannot enable TDE for the secondary clusters in a GDN. Used to view user KMS activation status.
                    type: string
                  tdeStatus:
                    description: |-
                      turn on TDE encryption. Valid values are Enabled, Disabled. Default to Disabled. TDE cannot be closed after it is turned on.
                      -> NOTE: tde_status Cannot modify after created when db_type is PostgreSQL or Oracle.tde_status only support modification from Disabled to Enabled when db_type is MySQL.
                    type: string
                  upgradeType:
                    description: Version upgrade type. Valid values are PROXY, DB,
                      ALL. PROXY means upgrading the proxy version, DB means upgrading
                      the db version, ALL means upgrading both db and proxy versions
                      simultaneously.
                    type: string
                  vpcId:
                    description: The id of the VPC.
                    type: string
                  vswitchId:
                    description: |-
                      The virtual switch ID to launch DB instances in one VPC.
                      -> NOTE: If vswitch_id is not specified, system will get a vswitch belongs to the user automatically.
                    type: string
                  zoneId:
                    description: The Zone to launch the DB cluster. it supports multiple
                      zone.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
