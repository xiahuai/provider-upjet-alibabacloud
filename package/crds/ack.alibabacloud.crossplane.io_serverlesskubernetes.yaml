---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: serverlesskubernetes.ack.alibabacloud.crossplane.io
spec:
  group: ack.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: ServerlessKubernetes
    listKind: ServerlessKubernetesList
    plural: serverlesskubernetes
    singular: serverlesskubernetes
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerlessKubernetes is the Schema for the ServerlessKubernetess
          API. Provides a Alicloud resource to manage container serverless kubernetes
          cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerlessKubernetesSpec defines the desired state of ServerlessKubernetes
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addons:
                    description: You can specific network plugin, log component, ingress
                      component and so on. See addons below. Only works for Create
                      Operation, use resource cs_kubernetes_addon to manage addons
                      if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of serverless kubernetes
                      cluster, which can be empty. Valid values:'
                    type: string
                  createV2Cluster:
                    description: (Removed since v1.229.1) whether to create a v2 version
                      cluster.
                    type: boolean
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether enable the deletion protection or not.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for version
                      1.22.3+. Default to false. Once the RRSA function is turned
                      on, it is not allowed to turn off. If your cluster has enabled
                      this function, please manually modify your tf file and add the
                      rrsa configuration to the file, learn more RAM Roles for Service
                      Accounts.
                    type: boolean
                  endpointPublicAccessEnabled:
                    description: Whether to create internet eip for API Server. Default
                      to false. Only works for Create Operation.
                    type: boolean
                  forceUpdate:
                    description: (Removed since v1.229.1) Default false, when you
                      want to change vpc_id and vswitch_id, you have to set this field
                      to true, then the cluster will be recreated.
                    type: boolean
                  kubeConfig:
                    description: The path of kube config, like ~/.kube/config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balance instance specification,
                      default slb.s2.small. For more information on how to select
                      a LB instance specification, see SLB instance overview. Only
                      works for Create Operation.
                    type: string
                  loggingType:
                    description: Enable log service, Valid value SLS. Only works for
                      Create Operation.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is the only in
                      one Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. SNAT must be configured when a new VPC is
                      automatically created. Default is true.
                    type: boolean
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  privateZone:
                    description: (Deprecated since v1.123.1) Has been deprecated from
                      provider version 1.123.1. PrivateZone is used as the enumeration
                      value of service_discovery_types.
                    type: boolean
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceCidr:
                    description: CIDR block of the service network. The specified
                      CIDR block cannot overlap with that of the VPC or those of the
                      ACK clusters that are deployed in the VPC. The CIDR block cannot
                      be modified after the cluster is created.
                    type: string
                  serviceDiscoveryTypes:
                    description: Service discovery type. Only works for Create Operation.
                      If the value is empty, it means that service discovery is not
                      enabled. Valid values are CoreDNS and PrivateZone.
                    items:
                      type: string
                    type: array
                  slsProjectName:
                    description: If you use an existing SLS project, you must specify
                      sls_project_name. Only works for Create Operation.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timeZone:
                    description: The time zone of the cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used.  Do not specify if cluster auto upgrade is enabled, see
                      cluster_auto_upgrade for more information.
                    type: string
                  vpcId:
                    description: The vpc where new kubernetes cluster will be located.
                      Specify one vpc's id, if it is not specified, a new VPC will
                      be built.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: (Removed since v1.229.1) The vswitch where new kubernetes
                      cluster will be located. Specify one vswitch's id, if it is
                      not specified, a new VPC and VSwicth will be built. It must
                      be in the zone which availability_zone specified.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIds:
                    description: The vswitches where new kubernetes cluster will be
                      located.
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneId:
                    description: When creating a cluster using automatic VPC creation,
                      you need to specify the zone where the VPC is located. Only
                      works for Create Operation.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: You can specific network plugin, log component, ingress
                      component and so on. See addons below. Only works for Create
                      Operation, use resource cs_kubernetes_addon to manage addons
                      if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of serverless kubernetes
                      cluster, which can be empty. Valid values:'
                    type: string
                  createV2Cluster:
                    description: (Removed since v1.229.1) whether to create a v2 version
                      cluster.
                    type: boolean
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether enable the deletion protection or not.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for version
                      1.22.3+. Default to false. Once the RRSA function is turned
                      on, it is not allowed to turn off. If your cluster has enabled
                      this function, please manually modify your tf file and add the
                      rrsa configuration to the file, learn more RAM Roles for Service
                      Accounts.
                    type: boolean
                  endpointPublicAccessEnabled:
                    description: Whether to create internet eip for API Server. Default
                      to false. Only works for Create Operation.
                    type: boolean
                  forceUpdate:
                    description: (Removed since v1.229.1) Default false, when you
                      want to change vpc_id and vswitch_id, you have to set this field
                      to true, then the cluster will be recreated.
                    type: boolean
                  kubeConfig:
                    description: The path of kube config, like ~/.kube/config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balance instance specification,
                      default slb.s2.small. For more information on how to select
                      a LB instance specification, see SLB instance overview. Only
                      works for Create Operation.
                    type: string
                  loggingType:
                    description: Enable log service, Valid value SLS. Only works for
                      Create Operation.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is the only in
                      one Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. SNAT must be configured when a new VPC is
                      automatically created. Default is true.
                    type: boolean
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  privateZone:
                    description: (Deprecated since v1.123.1) Has been deprecated from
                      provider version 1.123.1. PrivateZone is used as the enumeration
                      value of service_discovery_types.
                    type: boolean
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceCidr:
                    description: CIDR block of the service network. The specified
                      CIDR block cannot overlap with that of the VPC or those of the
                      ACK clusters that are deployed in the VPC. The CIDR block cannot
                      be modified after the cluster is created.
                    type: string
                  serviceDiscoveryTypes:
                    description: Service discovery type. Only works for Create Operation.
                      If the value is empty, it means that service discovery is not
                      enabled. Valid values are CoreDNS and PrivateZone.
                    items:
                      type: string
                    type: array
                  slsProjectName:
                    description: If you use an existing SLS project, you must specify
                      sls_project_name. Only works for Create Operation.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timeZone:
                    description: The time zone of the cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used.  Do not specify if cluster auto upgrade is enabled, see
                      cluster_auto_upgrade for more information.
                    type: string
                  vpcId:
                    description: The vpc where new kubernetes cluster will be located.
                      Specify one vpc's id, if it is not specified, a new VPC will
                      be built.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: (Removed since v1.229.1) The vswitch where new kubernetes
                      cluster will be located. Specify one vswitch's id, if it is
                      not specified, a new VPC and VSwicth will be built. It must
                      be in the zone which availability_zone specified.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIds:
                    description: The vswitches where new kubernetes cluster will be
                      located.
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneId:
                    description: When creating a cluster using automatic VPC creation,
                      you need to specify the zone where the VPC is located. Only
                      works for Create Operation.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ServerlessKubernetesStatus defines the observed state of
              ServerlessKubernetes.
            properties:
              atProvider:
                properties:
                  addons:
                    description: You can specific network plugin, log component, ingress
                      component and so on. See addons below. Only works for Create
                      Operation, use resource cs_kubernetes_addon to manage addons
                      if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of serverless kubernetes
                      cluster, which can be empty. Valid values:'
                    type: string
                  createV2Cluster:
                    description: (Removed since v1.229.1) whether to create a v2 version
                      cluster.
                    type: boolean
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether enable the deletion protection or not.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for version
                      1.22.3+. Default to false. Once the RRSA function is turned
                      on, it is not allowed to turn off. If your cluster has enabled
                      this function, please manually modify your tf file and add the
                      rrsa configuration to the file, learn more RAM Roles for Service
                      Accounts.
                    type: boolean
                  endpointPublicAccessEnabled:
                    description: Whether to create internet eip for API Server. Default
                      to false. Only works for Create Operation.
                    type: boolean
                  forceUpdate:
                    description: (Removed since v1.229.1) Default false, when you
                      want to change vpc_id and vswitch_id, you have to set this field
                      to true, then the cluster will be recreated.
                    type: boolean
                  id:
                    description: The Cluster ID of the serverless cluster.
                    type: string
                  kubeConfig:
                    description: The path of kube config, like ~/.kube/config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balance instance specification,
                      default slb.s2.small. For more information on how to select
                      a LB instance specification, see SLB instance overview. Only
                      works for Create Operation.
                    type: string
                  loggingType:
                    description: Enable log service, Valid value SLS. Only works for
                      Create Operation.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is the only in
                      one Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. SNAT must be configured when a new VPC is
                      automatically created. Default is true.
                    type: boolean
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  privateZone:
                    description: (Deprecated since v1.123.1) Has been deprecated from
                      provider version 1.123.1. PrivateZone is used as the enumeration
                      value of service_discovery_types.
                    type: boolean
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  rrsaMetadata:
                    description: Nested attribute containing RRSA related data for
                      your cluster.
                    items:
                      properties:
                        enabled:
                          description: Whether to enable automatic cluster upgrade.
                          type: boolean
                        ramOidcProviderArn:
                          description: The arn of OIDC provider that was registered
                            in RAM.
                          type: string
                        ramOidcProviderName:
                          description: The name of OIDC Provider that was registered
                            in RAM.
                          type: string
                        rrsaOidcIssuerUrl:
                          description: The issuer URL of RRSA OIDC Token.
                          type: string
                      type: object
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  serviceCidr:
                    description: CIDR block of the service network. The specified
                      CIDR block cannot overlap with that of the VPC or those of the
                      ACK clusters that are deployed in the VPC. The CIDR block cannot
                      be modified after the cluster is created.
                    type: string
                  serviceDiscoveryTypes:
                    description: Service discovery type. Only works for Create Operation.
                      If the value is empty, it means that service discovery is not
                      enabled. Valid values are CoreDNS and PrivateZone.
                    items:
                      type: string
                    type: array
                  slsProjectName:
                    description: If you use an existing SLS project, you must specify
                      sls_project_name. Only works for Create Operation.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timeZone:
                    description: The time zone of the cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used.  Do not specify if cluster auto upgrade is enabled, see
                      cluster_auto_upgrade for more information.
                    type: string
                  vpcId:
                    description: The vpc where new kubernetes cluster will be located.
                      Specify one vpc's id, if it is not specified, a new VPC will
                      be built.
                    type: string
                  vswitchId:
                    description: (Removed since v1.229.1) The vswitch where new kubernetes
                      cluster will be located. Specify one vswitch's id, if it is
                      not specified, a new VPC and VSwicth will be built. It must
                      be in the zone which availability_zone specified.
                    type: string
                  vswitchIds:
                    description: The vswitches where new kubernetes cluster will be
                      located.
                    items:
                      type: string
                    type: array
                  zoneId:
                    description: When creating a cluster using automatic VPC creation,
                      you need to specify the zone where the VPC is located. Only
                      works for Create Operation.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
