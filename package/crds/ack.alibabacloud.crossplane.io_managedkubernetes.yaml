---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: managedkubernetes.ack.alibabacloud.crossplane.io
spec:
  group: ack.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: ManagedKubernetes
    listKind: ManagedKubernetesList
    plural: managedkubernetes
    singular: managedkubernetes
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedKubernetes is the Schema for the ManagedKubernetess API.
          Provides a Alicloud resource to manage container managed kubernetes cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedKubernetesSpec defines the desired state of ManagedKubernetes
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  availabilityZone:
                    description: (Removed since v1.212.0) The Zone where new kubernetes
                      cluster will be located. If it is not be specified, the vswitch_ids
                      should be set, its value will be vswitch's zone.
                    type: string
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterNetworkType:
                    description: (Removed) The network that cluster uses, use flannel
                      or terway.
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,which
                      can be empty. Valid values:'
                    type: string
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  cpuPolicy:
                    description: (Removed since v1.212.0) Kubelet cpu policy. For
                      Kubernetes 1.12.6 and later, its valid value is either static
                      or none. Default to none.
                    type: string
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  enableSsh:
                    description: (Removed since v1.212.0) Enable login to the node
                      through SSH. Default to false.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The disk encryption key.
                      disk encryption key, only in ack-pro
                    type: string
                  excludeAutoscalerNodes:
                    description: (Removed since v1.212.0) Exclude autoscaler nodes
                      from worker_nodes. Default to false.
                    type: boolean
                  forceUpdate:
                    description: (Removed) Whether to force the update of kubernetes
                      cluster arguments. Default to false.
                    type: boolean
                  imageId:
                    description: (Removed since v1.212.0) Custom Image support. Must
                      based on CentOS7 or AliyunLinux2.
                    type: string
                  installCloudMonitor:
                    description: (Removed since v1.212.0) Install cloud monitor agent
                      on ECS. Default is true in previous version. From provider version
                      1.208.0, the default value is false.
                    type: boolean
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  keyName:
                    description: (Removed since v1.212.0) The keypair of ssh login
                      cluster node, you have to create it first. You have to specify
                      one of password key_name kms_encrypted_password fields. From
                      ersion 1.109.1, It is not necessary in the professional managed
                      cluster.
                    type: string
                  kmsEncryptedPassword:
                    description: (Removed since v1.212.0) An KMS encrypts password
                      used to a cs kubernetes. You have to specify one of password
                      key_name kms_encrypted_password fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) An KMS encryption context
                      used to decrypt kms_encrypted_password before creating or updating
                      a cs kubernetes with kms_encrypted_password. See Encryption
                      Context. It is valid when kms_encrypted_password is set.
                    type: object
                    x-kubernetes-map-type: granular
                  kubeConfig:
                    description: (Removed since v1.212.0) The path of kube config,
                      like ~/.kube/config. You can set some file paths to save kube_config
                      information, but this way is cumbersome. Since version 1.105.0,
                      we've written it to tf state file. About its use，see export
                      attribute certificate_authority. From version 1.187.0, new DataSource
                      alicloud_cs_cluster_credential is recommended to manage cluster's
                      kube_config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  logConfig:
                    description: (Removed) A list of one element containing information
                      about the associated log store. See log_config below.
                    items:
                      properties:
                        project:
                          description: Log Service project name, cluster logs will
                            output to this project.
                          type: string
                        type:
                          description: Type of collecting logs, only SLS are supported
                            currently.
                          type: string
                      type: object
                    type: array
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: (Removed since v1.212.0) Each node name consists
                      of a prefix, an IP substring, and a suffix, the input format
                      is customized,<prefix>,IPSubStringLen,<suffix>. For example
                      "customized,aliyun.com-,5,-test", if the node IP address is
                      192.168.59.176, the prefix is aliyun.com-, IP substring length
                      is 5, and the suffix is -test, the node name will be aliyun.com-59176-test.
                    type: string
                  nodePortRange:
                    description: '(Removed since v1.212.0) The service port range
                      of nodes, valid values: 30000 to 65535. Default to 30000-32767.'
                    type: string
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  osType:
                    description: (Removed since v1.212.0) The operating system of
                      the nodes that run pods, its valid value is either Linux or
                      Windows. Default to Linux.
                    type: string
                  passwordSecretRef:
                    description: (Removed since v1.212.0) The password of ssh login
                      cluster node. You have to specify one of password key_name kms_encrypted_password
                      fields. From ersion 1.109.1, It is not necessary in the professional
                      managed cluster.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  platform:
                    description: (Removed since v1.212.0) The architecture of the
                      nodes that run pods, its valid value is either CentOS or AliyunLinux.
                      Default to CentOS.
                    type: string
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  proxyMode:
                    description: 'Proxy mode is option of kube-proxy. options: iptables|ipvs.
                      default: ipvs.'
                    type: string
                  rdsInstances:
                    description: (Removed since v1.212.0) RDS instance list, You can
                      choose which RDS instances whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  runtime:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) The runtime of containers.
                      If you select another container runtime, see Comparison of Docker,
                      containerd, and Sandboxed-Container. See runtime below.
                    type: object
                    x-kubernetes-map-type: granular
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  taints:
                    description: (Removed since v1.212.0) Taints ensure pods are not
                      scheduled onto inappropriate nodes. One or more taints are applied
                      to a node; this marks that the node should not accept any pods
                      that do not tolerate the taints. For more information, see Taints
                      and Tolerations. See taints below.
                    items:
                      properties:
                        effect:
                          description: The taint effect.
                          type: string
                        key:
                          description: The taint key.
                          type: string
                        value:
                          description: The taint value.
                          type: string
                      type: object
                    type: array
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  userData:
                    description: (Removed since v1.212.0) Custom data that can execute
                      on nodes. For more information, see Prepare user data.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vswitchIds:
                    description: |-
                      The vSwitches of the control plane.
                      -> NOTE: Please take of note before updating the vswitch_ids:
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  workerAutoRenew:
                    description: (Removed since v1.212.0) Enable worker payment auto-renew,
                      defaults to false.
                    type: boolean
                  workerAutoRenewPeriod:
                    description: (Removed since v1.212.0) Worker payment auto-renew
                      period, it can be one of {1, 2, 3, 6, 12}.
                    type: number
                  workerDataDiskCategory:
                    description: (Removed) The data disk category of worker, use worker_data_disks
                      to instead it.
                    type: string
                  workerDataDiskSize:
                    description: (Removed) The data disk size of worker, use worker_data_disks
                      to instead it.
                    type: number
                  workerDataDisks:
                    description: (Removed since v1.212.0) The data disk configurations
                      of worker nodes, such as the disk type and disk size.  See worker_data_disks
                      below.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: Worker node data disk auto snapshot policy.
                          type: string
                        category:
                          description: 'The type of the data disks. Valid values:
                            cloud, cloud_efficiency, cloud_ssd and cloud_essd. Default
                            to cloud_efficiency.'
                          type: string
                        device:
                          description: The mount point of data disk N.
                          type: string
                        encrypted:
                          description: 'Specifies whether to encrypt data disks. Valid
                            values: true and false. Default to false.'
                          type: string
                        kmsKeyId:
                          description: The ID of the Key Management Service (KMS)
                            key to use for data disk N.
                          type: string
                        name:
                          description: The name of data disk N. The name must be 2
                            to 128 characters in length. It must start with a letter
                            and cannot start with http:// or https://. It can contain
                            letters, digits, colons (.), underscores (_), and hyphens
                            (-).
                          type: string
                        performanceLevel:
                          description: Worker node data disk performance level, when
                            category values cloud_essd, the optional values are PL0,
                            PL1, PL2 or PL3, but the specific performance level is
                            related to the disk capacity. For more information, see
                            Enhanced SSDs. Default is PL1.
                          type: string
                        size:
                          description: 'The size of a data disk, at least 40. Unit:
                            GiB.'
                          type: string
                        snapshotId:
                          description: 'The ID of the snapshot to be used to create
                            data disk N. Valid values of N: 1 to 16. When DataDisk.N.SnapshotId
                            is specified, DataDisk.N.Size is ignored. The data disk
                            is created based on the size of the specified snapshot.
                            Use snapshots that were created on or after July 15, 2013.
                            Otherwise, an error is returned and your request is rejected.'
                          type: string
                      type: object
                    type: array
                  workerDiskCategory:
                    description: (Removed since v1.212.0) The system disk category
                      of worker node. Its valid value are cloud, cloud_ssd, cloud_essd
                      and cloud_efficiency. Default to cloud_efficiency.
                    type: string
                  workerDiskPerformanceLevel:
                    description: (Removed since v1.212.0) Worker node system disk
                      performance level, when worker_disk_category values cloud_essd,
                      the optional values are PL0, PL1, PL2 or PL3, but the specific
                      performance level is related to the disk capacity. For more
                      information, see Enhanced SSDs. Default is PL1.
                    type: string
                  workerDiskSize:
                    description: (Removed since v1.212.0) The system disk size of
                      worker node. Its valid value range [40~500] in GB.
                    type: number
                  workerDiskSnapshotPolicyId:
                    description: (Removed since v1.212.0) Worker node system disk
                      auto snapshot policy.
                    type: string
                  workerInstanceChargeType:
                    description: (Removed since v1.212.0) Worker payment type, its
                      valid value is either or PostPaid or PrePaid. Defaults to PostPaid.
                      If value is PrePaid, the files worker_period, worker_period_unit,
                      worker_auto_renew and worker_auto_renew_period are required,
                      default is PostPaid.
                    type: string
                  workerInstanceType:
                    description: (Removed from version 1.16.0) The instance type of
                      worker node.
                    type: string
                  workerInstanceTypes:
                    description: (Removed since v1.212.0) The instance type of worker
                      node. Specify one type for single AZ Cluster, three types for
                      MultiAZ Cluster. From version 1.109.1, It is not necessary in
                      the professional managed cluster, but it is necessary in other
                      types of clusters.
                    items:
                      type: string
                    type: array
                  workerNodes:
                    description: (Removed from version 1.212.0) List of cluster worker
                      nodes.
                    items:
                      type: object
                    type: array
                  workerNumber:
                    description: (Removed since v1.212.0) The worker node number of
                      the kubernetes cluster. Default to 3. It is limited up to 50
                      and if you want to enlarge it, please apply white list or contact
                      with us. From version 1.109.1, It is not necessary in the professional
                      managed cluster, but it is necessary in other types of clusters.
                    type: number
                  workerNumbers:
                    description: (Removed) The number of workers, use worker_number
                      to instead it.
                    items:
                      type: number
                    type: array
                  workerPeriod:
                    description: (Removed since v1.212.0) Worker payment period. The
                      unit is Month. Its valid value is one of {1, 2, 3, 6, 12, 24,
                      36, 48, 60}.
                    type: number
                  workerPeriodUnit:
                    description: (Removed since v1.212.0) Worker payment period unit,
                      the valid value is Month.
                    type: string
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  availabilityZone:
                    description: (Removed since v1.212.0) The Zone where new kubernetes
                      cluster will be located. If it is not be specified, the vswitch_ids
                      should be set, its value will be vswitch's zone.
                    type: string
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterNetworkType:
                    description: (Removed) The network that cluster uses, use flannel
                      or terway.
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,which
                      can be empty. Valid values:'
                    type: string
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  cpuPolicy:
                    description: (Removed since v1.212.0) Kubelet cpu policy. For
                      Kubernetes 1.12.6 and later, its valid value is either static
                      or none. Default to none.
                    type: string
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  enableSsh:
                    description: (Removed since v1.212.0) Enable login to the node
                      through SSH. Default to false.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The disk encryption key.
                      disk encryption key, only in ack-pro
                    type: string
                  excludeAutoscalerNodes:
                    description: (Removed since v1.212.0) Exclude autoscaler nodes
                      from worker_nodes. Default to false.
                    type: boolean
                  forceUpdate:
                    description: (Removed) Whether to force the update of kubernetes
                      cluster arguments. Default to false.
                    type: boolean
                  imageId:
                    description: (Removed since v1.212.0) Custom Image support. Must
                      based on CentOS7 or AliyunLinux2.
                    type: string
                  installCloudMonitor:
                    description: (Removed since v1.212.0) Install cloud monitor agent
                      on ECS. Default is true in previous version. From provider version
                      1.208.0, the default value is false.
                    type: boolean
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  keyName:
                    description: (Removed since v1.212.0) The keypair of ssh login
                      cluster node, you have to create it first. You have to specify
                      one of password key_name kms_encrypted_password fields. From
                      ersion 1.109.1, It is not necessary in the professional managed
                      cluster.
                    type: string
                  kmsEncryptedPassword:
                    description: (Removed since v1.212.0) An KMS encrypts password
                      used to a cs kubernetes. You have to specify one of password
                      key_name kms_encrypted_password fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) An KMS encryption context
                      used to decrypt kms_encrypted_password before creating or updating
                      a cs kubernetes with kms_encrypted_password. See Encryption
                      Context. It is valid when kms_encrypted_password is set.
                    type: object
                    x-kubernetes-map-type: granular
                  kubeConfig:
                    description: (Removed since v1.212.0) The path of kube config,
                      like ~/.kube/config. You can set some file paths to save kube_config
                      information, but this way is cumbersome. Since version 1.105.0,
                      we've written it to tf state file. About its use，see export
                      attribute certificate_authority. From version 1.187.0, new DataSource
                      alicloud_cs_cluster_credential is recommended to manage cluster's
                      kube_config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  logConfig:
                    description: (Removed) A list of one element containing information
                      about the associated log store. See log_config below.
                    items:
                      properties:
                        project:
                          description: Log Service project name, cluster logs will
                            output to this project.
                          type: string
                        type:
                          description: Type of collecting logs, only SLS are supported
                            currently.
                          type: string
                      type: object
                    type: array
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: (Removed since v1.212.0) Each node name consists
                      of a prefix, an IP substring, and a suffix, the input format
                      is customized,<prefix>,IPSubStringLen,<suffix>. For example
                      "customized,aliyun.com-,5,-test", if the node IP address is
                      192.168.59.176, the prefix is aliyun.com-, IP substring length
                      is 5, and the suffix is -test, the node name will be aliyun.com-59176-test.
                    type: string
                  nodePortRange:
                    description: '(Removed since v1.212.0) The service port range
                      of nodes, valid values: 30000 to 65535. Default to 30000-32767.'
                    type: string
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  osType:
                    description: (Removed since v1.212.0) The operating system of
                      the nodes that run pods, its valid value is either Linux or
                      Windows. Default to Linux.
                    type: string
                  passwordSecretRef:
                    description: (Removed since v1.212.0) The password of ssh login
                      cluster node. You have to specify one of password key_name kms_encrypted_password
                      fields. From ersion 1.109.1, It is not necessary in the professional
                      managed cluster.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  platform:
                    description: (Removed since v1.212.0) The architecture of the
                      nodes that run pods, its valid value is either CentOS or AliyunLinux.
                      Default to CentOS.
                    type: string
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  proxyMode:
                    description: 'Proxy mode is option of kube-proxy. options: iptables|ipvs.
                      default: ipvs.'
                    type: string
                  rdsInstances:
                    description: (Removed since v1.212.0) RDS instance list, You can
                      choose which RDS instances whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  runtime:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) The runtime of containers.
                      If you select another container runtime, see Comparison of Docker,
                      containerd, and Sandboxed-Container. See runtime below.
                    type: object
                    x-kubernetes-map-type: granular
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  taints:
                    description: (Removed since v1.212.0) Taints ensure pods are not
                      scheduled onto inappropriate nodes. One or more taints are applied
                      to a node; this marks that the node should not accept any pods
                      that do not tolerate the taints. For more information, see Taints
                      and Tolerations. See taints below.
                    items:
                      properties:
                        effect:
                          description: The taint effect.
                          type: string
                        key:
                          description: The taint key.
                          type: string
                        value:
                          description: The taint value.
                          type: string
                      type: object
                    type: array
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  userData:
                    description: (Removed since v1.212.0) Custom data that can execute
                      on nodes. For more information, see Prepare user data.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vswitchIds:
                    description: |-
                      The vSwitches of the control plane.
                      -> NOTE: Please take of note before updating the vswitch_ids:
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  workerAutoRenew:
                    description: (Removed since v1.212.0) Enable worker payment auto-renew,
                      defaults to false.
                    type: boolean
                  workerAutoRenewPeriod:
                    description: (Removed since v1.212.0) Worker payment auto-renew
                      period, it can be one of {1, 2, 3, 6, 12}.
                    type: number
                  workerDataDiskCategory:
                    description: (Removed) The data disk category of worker, use worker_data_disks
                      to instead it.
                    type: string
                  workerDataDiskSize:
                    description: (Removed) The data disk size of worker, use worker_data_disks
                      to instead it.
                    type: number
                  workerDataDisks:
                    description: (Removed since v1.212.0) The data disk configurations
                      of worker nodes, such as the disk type and disk size.  See worker_data_disks
                      below.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: Worker node data disk auto snapshot policy.
                          type: string
                        category:
                          description: 'The type of the data disks. Valid values:
                            cloud, cloud_efficiency, cloud_ssd and cloud_essd. Default
                            to cloud_efficiency.'
                          type: string
                        device:
                          description: The mount point of data disk N.
                          type: string
                        encrypted:
                          description: 'Specifies whether to encrypt data disks. Valid
                            values: true and false. Default to false.'
                          type: string
                        kmsKeyId:
                          description: The ID of the Key Management Service (KMS)
                            key to use for data disk N.
                          type: string
                        name:
                          description: The name of data disk N. The name must be 2
                            to 128 characters in length. It must start with a letter
                            and cannot start with http:// or https://. It can contain
                            letters, digits, colons (.), underscores (_), and hyphens
                            (-).
                          type: string
                        performanceLevel:
                          description: Worker node data disk performance level, when
                            category values cloud_essd, the optional values are PL0,
                            PL1, PL2 or PL3, but the specific performance level is
                            related to the disk capacity. For more information, see
                            Enhanced SSDs. Default is PL1.
                          type: string
                        size:
                          description: 'The size of a data disk, at least 40. Unit:
                            GiB.'
                          type: string
                        snapshotId:
                          description: 'The ID of the snapshot to be used to create
                            data disk N. Valid values of N: 1 to 16. When DataDisk.N.SnapshotId
                            is specified, DataDisk.N.Size is ignored. The data disk
                            is created based on the size of the specified snapshot.
                            Use snapshots that were created on or after July 15, 2013.
                            Otherwise, an error is returned and your request is rejected.'
                          type: string
                      type: object
                    type: array
                  workerDiskCategory:
                    description: (Removed since v1.212.0) The system disk category
                      of worker node. Its valid value are cloud, cloud_ssd, cloud_essd
                      and cloud_efficiency. Default to cloud_efficiency.
                    type: string
                  workerDiskPerformanceLevel:
                    description: (Removed since v1.212.0) Worker node system disk
                      performance level, when worker_disk_category values cloud_essd,
                      the optional values are PL0, PL1, PL2 or PL3, but the specific
                      performance level is related to the disk capacity. For more
                      information, see Enhanced SSDs. Default is PL1.
                    type: string
                  workerDiskSize:
                    description: (Removed since v1.212.0) The system disk size of
                      worker node. Its valid value range [40~500] in GB.
                    type: number
                  workerDiskSnapshotPolicyId:
                    description: (Removed since v1.212.0) Worker node system disk
                      auto snapshot policy.
                    type: string
                  workerInstanceChargeType:
                    description: (Removed since v1.212.0) Worker payment type, its
                      valid value is either or PostPaid or PrePaid. Defaults to PostPaid.
                      If value is PrePaid, the files worker_period, worker_period_unit,
                      worker_auto_renew and worker_auto_renew_period are required,
                      default is PostPaid.
                    type: string
                  workerInstanceType:
                    description: (Removed from version 1.16.0) The instance type of
                      worker node.
                    type: string
                  workerInstanceTypes:
                    description: (Removed since v1.212.0) The instance type of worker
                      node. Specify one type for single AZ Cluster, three types for
                      MultiAZ Cluster. From version 1.109.1, It is not necessary in
                      the professional managed cluster, but it is necessary in other
                      types of clusters.
                    items:
                      type: string
                    type: array
                  workerNodes:
                    description: (Removed from version 1.212.0) List of cluster worker
                      nodes.
                    items:
                      type: object
                    type: array
                  workerNumber:
                    description: (Removed since v1.212.0) The worker node number of
                      the kubernetes cluster. Default to 3. It is limited up to 50
                      and if you want to enlarge it, please apply white list or contact
                      with us. From version 1.109.1, It is not necessary in the professional
                      managed cluster, but it is necessary in other types of clusters.
                    type: number
                  workerNumbers:
                    description: (Removed) The number of workers, use worker_number
                      to instead it.
                    items:
                      type: number
                    type: array
                  workerPeriod:
                    description: (Removed since v1.212.0) Worker payment period. The
                      unit is Month. Its valid value is one of {1, 2, 3, 6, 12, 24,
                      36, 48, 60}.
                    type: number
                  workerPeriodUnit:
                    description: (Removed since v1.212.0) Worker payment period unit,
                      the valid value is Month.
                    type: string
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ManagedKubernetesStatus defines the observed state of ManagedKubernetes.
            properties:
              atProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  availabilityZone:
                    description: (Removed since v1.212.0) The Zone where new kubernetes
                      cluster will be located. If it is not be specified, the vswitch_ids
                      should be set, its value will be vswitch's zone.
                    type: string
                  certificateAuthority:
                    additionalProperties:
                      type: string
                    description: (Available since v1.105.0) Nested attribute containing
                      certificate authority data for your cluster.
                    type: object
                    x-kubernetes-map-type: granular
                  clientCert:
                    description: The path of client certificate, like ~/.kube/client-cert.pem.
                    type: string
                  clientKey:
                    description: The path of client key, like ~/.kube/client-key.pem.
                    type: string
                  clusterCaCert:
                    description: The path of cluster ca certificate, like ~/.kube/cluster-ca-cert.pem
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterNetworkType:
                    description: (Removed) The network that cluster uses, use flannel
                      or terway.
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,which
                      can be empty. Valid values:'
                    type: string
                  connections:
                    additionalProperties:
                      type: string
                    description: Map of kubernetes cluster connection information.
                    type: object
                    x-kubernetes-map-type: granular
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  cpuPolicy:
                    description: (Removed since v1.212.0) Kubelet cpu policy. For
                      Kubernetes 1.12.6 and later, its valid value is either static
                      or none. Default to none.
                    type: string
                  customSan:
                    description: |-
                      Customize the certificate SAN, multiple IP or domain names are separated by English commas (,).
                      -> NOTE: Make sure you have specified all certificate SANs before updating. Updating this field will lead APIServer to restart.
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  enableSsh:
                    description: (Removed since v1.212.0) Enable login to the node
                      through SSH. Default to false.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The disk encryption key.
                      disk encryption key, only in ack-pro
                    type: string
                  excludeAutoscalerNodes:
                    description: (Removed since v1.212.0) Exclude autoscaler nodes
                      from worker_nodes. Default to false.
                    type: boolean
                  forceUpdate:
                    description: (Removed) Whether to force the update of kubernetes
                      cluster arguments. Default to false.
                    type: boolean
                  id:
                    description: The ID of the container cluster.
                    type: string
                  imageId:
                    description: (Removed since v1.212.0) Custom Image support. Must
                      based on CentOS7 or AliyunLinux2.
                    type: string
                  installCloudMonitor:
                    description: (Removed since v1.212.0) Install cloud monitor agent
                      on ECS. Default is true in previous version. From provider version
                      1.208.0, the default value is false.
                    type: boolean
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  keyName:
                    description: (Removed since v1.212.0) The keypair of ssh login
                      cluster node, you have to create it first. You have to specify
                      one of password key_name kms_encrypted_password fields. From
                      ersion 1.109.1, It is not necessary in the professional managed
                      cluster.
                    type: string
                  kmsEncryptedPassword:
                    description: (Removed since v1.212.0) An KMS encrypts password
                      used to a cs kubernetes. You have to specify one of password
                      key_name kms_encrypted_password fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) An KMS encryption context
                      used to decrypt kms_encrypted_password before creating or updating
                      a cs kubernetes with kms_encrypted_password. See Encryption
                      Context. It is valid when kms_encrypted_password is set.
                    type: object
                    x-kubernetes-map-type: granular
                  kubeConfig:
                    description: (Removed since v1.212.0) The path of kube config,
                      like ~/.kube/config. You can set some file paths to save kube_config
                      information, but this way is cumbersome. Since version 1.105.0,
                      we've written it to tf state file. About its use，see export
                      attribute certificate_authority. From version 1.187.0, new DataSource
                      alicloud_cs_cluster_credential is recommended to manage cluster's
                      kube_config.
                    type: string
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  logConfig:
                    description: (Removed) A list of one element containing information
                      about the associated log store. See log_config below.
                    items:
                      properties:
                        project:
                          description: Log Service project name, cluster logs will
                            output to this project.
                          type: string
                        type:
                          description: Type of collecting logs, only SLS are supported
                            currently.
                          type: string
                      type: object
                    type: array
                  maintenanceWindow:
                    description: The cluster maintenance window，effective only in
                      the professional managed cluster. Managed node pool will use
                      it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  natGatewayId:
                    description: The ID of nat gateway used to launch kubernetes cluster.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: (Removed since v1.212.0) Each node name consists
                      of a prefix, an IP substring, and a suffix, the input format
                      is customized,<prefix>,IPSubStringLen,<suffix>. For example
                      "customized,aliyun.com-,5,-test", if the node IP address is
                      192.168.59.176, the prefix is aliyun.com-, IP substring length
                      is 5, and the suffix is -test, the node name will be aliyun.com-59176-test.
                    type: string
                  nodePortRange:
                    description: '(Removed since v1.212.0) The service port range
                      of nodes, valid values: 30000 to 65535. Default to 30000-32767.'
                    type: string
                  operationPolicy:
                    description: The cluster automatic operation policy. See operation_policy
                      below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  osType:
                    description: (Removed since v1.212.0) The operating system of
                      the nodes that run pods, its valid value is either Linux or
                      Windows. Default to Linux.
                    type: string
                  platform:
                    description: (Removed since v1.212.0) The architecture of the
                      nodes that run pods, its valid value is either CentOS or AliyunLinux.
                      Default to CentOS.
                    type: string
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  proxyMode:
                    description: 'Proxy mode is option of kube-proxy. options: iptables|ipvs.
                      default: ipvs.'
                    type: string
                  rdsInstances:
                    description: (Removed since v1.212.0) RDS instance list, You can
                      choose which RDS instances whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  rrsaMetadata:
                    description: Nested attribute containing RRSA related data for
                      your cluster.
                    items:
                      properties:
                        enabled:
                          description: Whether to enable automatic cluster upgrade.
                          type: boolean
                        ramOidcProviderArn:
                          description: The arn of OIDC provider that was registered
                            in RAM.
                          type: string
                        ramOidcProviderName:
                          description: The name of OIDC Provider that was registered
                            in RAM.
                          type: string
                        rrsaOidcIssuerUrl:
                          description: The issuer URL of RRSA OIDC Token.
                          type: string
                      type: object
                    type: array
                  runtime:
                    additionalProperties:
                      type: string
                    description: (Removed since v1.212.0) The runtime of containers.
                      If you select another container runtime, see Comparison of Docker,
                      containerd, and Sandboxed-Container. See runtime below.
                    type: object
                    x-kubernetes-map-type: granular
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  slbId:
                    description: The ID of APIServer load balancer.
                    type: string
                  slbInternet:
                    description: The public ip of load balancer.
                    type: string
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  slbIntranet:
                    description: The ID of private load balancer where the current
                      cluster master node is located.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  taints:
                    description: (Removed since v1.212.0) Taints ensure pods are not
                      scheduled onto inappropriate nodes. One or more taints are applied
                      to a node; this marks that the node should not accept any pods
                      that do not tolerate the taints. For more information, see Taints
                      and Tolerations. See taints below.
                    items:
                      properties:
                        effect:
                          description: The taint effect.
                          type: string
                        key:
                          description: The taint key.
                          type: string
                        value:
                          description: The taint value.
                          type: string
                      type: object
                    type: array
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  userData:
                    description: (Removed since v1.212.0) Custom data that can execute
                      on nodes. For more information, see Prepare user data.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vpcId:
                    description: The ID of VPC where the current cluster is located.
                    type: string
                  vswitchIds:
                    description: |-
                      The vSwitches of the control plane.
                      -> NOTE: Please take of note before updating the vswitch_ids:
                    items:
                      type: string
                    type: array
                  workerAutoRenew:
                    description: (Removed since v1.212.0) Enable worker payment auto-renew,
                      defaults to false.
                    type: boolean
                  workerAutoRenewPeriod:
                    description: (Removed since v1.212.0) Worker payment auto-renew
                      period, it can be one of {1, 2, 3, 6, 12}.
                    type: number
                  workerDataDiskCategory:
                    description: (Removed) The data disk category of worker, use worker_data_disks
                      to instead it.
                    type: string
                  workerDataDiskSize:
                    description: (Removed) The data disk size of worker, use worker_data_disks
                      to instead it.
                    type: number
                  workerDataDisks:
                    description: (Removed since v1.212.0) The data disk configurations
                      of worker nodes, such as the disk type and disk size.  See worker_data_disks
                      below.
                    items:
                      properties:
                        autoSnapshotPolicyId:
                          description: Worker node data disk auto snapshot policy.
                          type: string
                        category:
                          description: 'The type of the data disks. Valid values:
                            cloud, cloud_efficiency, cloud_ssd and cloud_essd. Default
                            to cloud_efficiency.'
                          type: string
                        device:
                          description: The mount point of data disk N.
                          type: string
                        encrypted:
                          description: 'Specifies whether to encrypt data disks. Valid
                            values: true and false. Default to false.'
                          type: string
                        kmsKeyId:
                          description: The ID of the Key Management Service (KMS)
                            key to use for data disk N.
                          type: string
                        name:
                          description: The name of data disk N. The name must be 2
                            to 128 characters in length. It must start with a letter
                            and cannot start with http:// or https://. It can contain
                            letters, digits, colons (.), underscores (_), and hyphens
                            (-).
                          type: string
                        performanceLevel:
                          description: Worker node data disk performance level, when
                            category values cloud_essd, the optional values are PL0,
                            PL1, PL2 or PL3, but the specific performance level is
                            related to the disk capacity. For more information, see
                            Enhanced SSDs. Default is PL1.
                          type: string
                        size:
                          description: 'The size of a data disk, at least 40. Unit:
                            GiB.'
                          type: string
                        snapshotId:
                          description: 'The ID of the snapshot to be used to create
                            data disk N. Valid values of N: 1 to 16. When DataDisk.N.SnapshotId
                            is specified, DataDisk.N.Size is ignored. The data disk
                            is created based on the size of the specified snapshot.
                            Use snapshots that were created on or after July 15, 2013.
                            Otherwise, an error is returned and your request is rejected.'
                          type: string
                      type: object
                    type: array
                  workerDiskCategory:
                    description: (Removed since v1.212.0) The system disk category
                      of worker node. Its valid value are cloud, cloud_ssd, cloud_essd
                      and cloud_efficiency. Default to cloud_efficiency.
                    type: string
                  workerDiskPerformanceLevel:
                    description: (Removed since v1.212.0) Worker node system disk
                      performance level, when worker_disk_category values cloud_essd,
                      the optional values are PL0, PL1, PL2 or PL3, but the specific
                      performance level is related to the disk capacity. For more
                      information, see Enhanced SSDs. Default is PL1.
                    type: string
                  workerDiskSize:
                    description: (Removed since v1.212.0) The system disk size of
                      worker node. Its valid value range [40~500] in GB.
                    type: number
                  workerDiskSnapshotPolicyId:
                    description: (Removed since v1.212.0) Worker node system disk
                      auto snapshot policy.
                    type: string
                  workerInstanceChargeType:
                    description: (Removed since v1.212.0) Worker payment type, its
                      valid value is either or PostPaid or PrePaid. Defaults to PostPaid.
                      If value is PrePaid, the files worker_period, worker_period_unit,
                      worker_auto_renew and worker_auto_renew_period are required,
                      default is PostPaid.
                    type: string
                  workerInstanceType:
                    description: (Removed from version 1.16.0) The instance type of
                      worker node.
                    type: string
                  workerInstanceTypes:
                    description: (Removed since v1.212.0) The instance type of worker
                      node. Specify one type for single AZ Cluster, three types for
                      MultiAZ Cluster. From version 1.109.1, It is not necessary in
                      the professional managed cluster, but it is necessary in other
                      types of clusters.
                    items:
                      type: string
                    type: array
                  workerNodes:
                    description: (Removed from version 1.212.0) List of cluster worker
                      nodes.
                    items:
                      properties:
                        id:
                          description: The ID of the container cluster.
                          type: string
                        name:
                          description: The name of data disk N. The name must be 2
                            to 128 characters in length. It must start with a letter
                            and cannot start with http:// or https://. It can contain
                            letters, digits, colons (.), underscores (_), and hyphens
                            (-).
                          type: string
                        privateIp:
                          description: The private IP address of node.
                          type: string
                      type: object
                    type: array
                  workerNumber:
                    description: (Removed since v1.212.0) The worker node number of
                      the kubernetes cluster. Default to 3. It is limited up to 50
                      and if you want to enlarge it, please apply white list or contact
                      with us. From version 1.109.1, It is not necessary in the professional
                      managed cluster, but it is necessary in other types of clusters.
                    type: number
                  workerNumbers:
                    description: (Removed) The number of workers, use worker_number
                      to instead it.
                    items:
                      type: number
                    type: array
                  workerPeriod:
                    description: (Removed since v1.212.0) Worker payment period. The
                      unit is Month. Its valid value is one of {1, 2, 3, 6, 12, 24,
                      36, 48, 60}.
                    type: number
                  workerPeriodUnit:
                    description: (Removed since v1.212.0) Worker payment period unit,
                      the valid value is Month.
                    type: string
                  workerRamRoleName:
                    description: The RamRole Name attached to worker node.
                    type: string
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
